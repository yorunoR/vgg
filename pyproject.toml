[project]
name = "vgg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "keras>=3.7.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.1",
]
